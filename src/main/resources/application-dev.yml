rate-service:
    base-url: http://localhost:8084/

flight-service:
    base-url: http://localhost:8083/

logging:
    level:
        webclient: DEBUG

spring:
    jpa:
        show-sql: true
        properties:
            hibernate:
                format_sql: true
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: "jdbc:mysql://localhost:3306/orders"
        username: "root"
        password: "SuSundron007@"

server:
    port: 8086
